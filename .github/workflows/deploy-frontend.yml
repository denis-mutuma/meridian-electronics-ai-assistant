name: deploy-frontend

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trigger Amplify deployment
        run: |
          echo "Amplify auto-build triggers on configured branch after terraform provisioning."
